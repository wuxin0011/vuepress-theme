import{_ as e,p as i,q as l,a1 as a}from"./framework-5866ffd3.js";const r={},d=a('<h1 id="测试" tabindex="-1"><a class="header-anchor" href="#测试" aria-hidden="true">#</a> 测试</h1><h2 id="测试1" tabindex="-1"><a class="header-anchor" href="#测试1" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-1" tabindex="-1"><a class="header-anchor" href="#测试1-1" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-2" tabindex="-1"><a class="header-anchor" href="#测试1-2" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-3" tabindex="-1"><a class="header-anchor" href="#测试1-3" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-4" tabindex="-1"><a class="header-anchor" href="#测试1-4" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-5" tabindex="-1"><a class="header-anchor" href="#测试1-5" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-6" tabindex="-1"><a class="header-anchor" href="#测试1-6" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-7" tabindex="-1"><a class="header-anchor" href="#测试1-7" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-8" tabindex="-1"><a class="header-anchor" href="#测试1-8" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-9" tabindex="-1"><a class="header-anchor" href="#测试1-9" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul><h2 id="测试1-10" tabindex="-1"><a class="header-anchor" href="#测试1-10" aria-hidden="true">#</a> 测试1</h2><p>上图展示了 VuePress 的简要架构：</p><ul><li>Node App 会生成临时文件，包括页面、路由等。</li><li>Bundler 会将 Client App 和临时文件一起进行打包，就像处理一个普通的 Vue SPA 一样。</li></ul>',34),h=[d];function u(n,p){return i(),l("div",null,h)}const t=e(r,[["render",u],["__file","02-test.html.vue"]]);export{t as default};
